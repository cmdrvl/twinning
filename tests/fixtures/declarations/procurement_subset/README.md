# procurement subset declaration fixture

This fixture models the declaration handed to twinning by an upstream metadata
catalog selection for a procurement outcome.

The declaration is deliberately identity-only. It names the selected catalog
tables, catalog/source fingerprints, outcome tags, table keys, and key shapes.
Twinning validates that the declaration matches the supplied schema but does not
resolve claims or contact the catalog.
